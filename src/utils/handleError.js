
// code错误处理
export function createError(response) {
	if (response.data.code == '-2100') {
		
	} 
}

// 请求失败处理
export function createHandleError(error) {
	switch (error.response.status) {
		case 401:
		
	}
}


